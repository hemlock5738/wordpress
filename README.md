# WordPress

WordPress development environment

## Usage

1. Generate and fill `.env`.
    ```sh
    mise run init
    ```

2. Start containers.
    ```sh
    mise run up
    ```

3. Visit http://localhost.
